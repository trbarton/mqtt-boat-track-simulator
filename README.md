# mqtt-boat-track-simulator

Creates an MQTT server and publishes track points to the 'boats' topic at a regular interval.
Used primarily for development of dinghy track application.

Usage:
* npm install
* node server.js
